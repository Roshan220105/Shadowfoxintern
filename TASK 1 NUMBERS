#code for 1st problem in numbers
def format_number(num, specifier):
    return format(num, specifier)

result = format_number(145, 'o')
print(result)
#output : 221
#explanation:The 'o' format specifier tells Python to convert the number to octal (base-8).
So, format(145, 'o') returns '221', which is the octal representation of the decimal number 145.


#code for the 2nd problem in numbers
r=84
pi=3.14
ca=pi*r*r
print(ca)
print(int(ca))
#output:22155.84
22155.84
#explanation:first value is normal value and the second is for without decimal points ,it is done with the help of int() function

#code for 3rd problem in numbers
distance = 490
time_minutes = 7
time_seconds = time_minutes * 60
speed = distance / time_seconds
print(int(speed))
#output:1
#explanation:The exact result of 490 / 420 ≈ 1.1667, but we print only the integer part using int(), which truncates the decimal.

